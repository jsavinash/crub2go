/** @format */

import {AppRegistry} from 'react-native';
import RootAppContainer from './src/RootAppContainer';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RootAppContainer);

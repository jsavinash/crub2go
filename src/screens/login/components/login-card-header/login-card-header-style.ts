import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: '1%'
    },
    title: {
        marginLeft: '5%',
        marginTop: '2%',
        fontSize: 16,
        color: 'black',
    },
    content: {
        color: '#ACD472',
        marginLeft: '5%',
        fontWeight: "500",
        fontSize: 24,
        marginBottom: 5
    },
    border: {
        borderBottomColor: '#DCDCDC',
        borderBottomWidth: 1,
    }
});
import { StyleSheet,  } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        backgroundColor: '#f3f3f3'
    },
    txt: {
        marginTop: '4%',
        marginBottom: '4%',
        left: '4%',
        fontSize: 16,
        color: 'black'
    }
});
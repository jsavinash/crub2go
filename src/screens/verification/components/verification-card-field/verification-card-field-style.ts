import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 0.7,
        margin: 14,
    },
    codeTxt: {
        color: '#31B404',
        fontSize: 18,
        fontWeight: '800',
        textAlign: 'center'
    },
});